@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // TODO:
        // Replace this hardcoded redirect with an IsLoggedIn()
        // check once authentication has been implemented.
        //
        // if (IsLoggedIn())
        // {
        //     Navigation.NavigateTo("/dashboard");
        // }
        // else
        // {
        //     Navigation.NavigateTo("/login");
        // }

        // Maybe here can navigate to login page instead of uncontained form
        // Navigation.NavigateTo("/login"); 
        Navigation.NavigateTo("/dashboard");
    }
}